--  CREATE TABLE useres (
--     user_id SERIAL PRIMARY KEY,
--  	first_name VARCHAR(40),
-- 	last_name VARCHAR(30),
-- 	email VARCHAR(50),
-- 	user_name TEXT NOT NULL,
-- 	password TEXT NOT NULL
--  )

--    CREATE TABLE albumes (
--       albume_id SERIAL PRIMARY KEY, 
-- 	   user_id_fk INTEGER  NOT NULL 
--   	 REFERENCES useres (user_id)
--       ON DELETE CASCADE 
--       ON UPDATE CASCADE,
--  	 albume_name TEXT NOT NULL
--    )

--    CREATE TABLE images (
--       image_id SERIAL PRIMARY KEY,
--       albume_id_fk INTEGER  NOT NULL 
--   	 REFERENCES albumes (albume_id)
--       ON DELETE CASCADE 
--       ON UPDATE CASCADE,
--  	 image_url TEXT NOT NULL,
-- 	  image_name TEXT NOT NULL
--    )


-- INSERT INTO useres(first_name, last_name, email, user_name, password) VALUES
--  ('Thor', 'Odinsun', 'OdinsunRull@gmail.com', 'Odinsun_storgenst_avenger', 1234568)

--  INSERT INTO albumes (user_id_fk ) VALUES
--   (1, 'Battle of new york')


--   INSERT INTO images (albume_id_fk,  image_url, image_name) VALUES
--    (1, 'https://res.cloudinary.com/dslk6mmz4/image/upload/v1639423808/fpocf1dhzfylqkuqgqqx.jpg', 'minion')
-- AND albumes.albume_id = images.albume_id


-- SELECT user_name, albume_name , image_url
-- FROM useres
-- INNER JOIN albumes ON useres.user_id = albumes.user_id_fk 
-- INNER JOIN images ON albumes.albume_id = images.albume_id_fk;





{/* <Box className="container" sx={{ boxShadow: 3 }}>
        <Box sx={{ mt: 6 }}>
            <Typography variant="h3" color="primary" sx={{ display: 'inline' }}>Welecome to</Typography>
            <Typography variant="h4" color="primary" sx={{ display: 'inline' }}> Memories book the place to store your memories, Lorem ipsum dolor sit amet consectetur adipisicing elit. Suscipit error qu</Typography>
            <Button variant="contained" color="primary" size="large" sx={{
                alignSelf: 'flex-end',
                width: "100%",
                mt: 20
            }}>lets start</Button>
        </Box>
        <Box>
            <Avatar src={logo} sx={{
                width: '20vw',
                height: '40vh',
                mt: 8,
                ml:2,
                boxShadow: 7
            }} />
        </Box>
    </Box> */}